# Training stack for jp (RTX 5090 Blackwell, Python 3.12, CUDA 12.8+).
# The sim stack stays pinned by environment.yml; this file only ADDS the
# learning stack. Blackwell (sm_120) needs torch >= 2.7; torchvision must
# match torch. Verify on jp with: python scripts/check_train_stack.py.
# Install (CUDA 12.8 wheels): pip install -r requirements-train.txt
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.7
torchvision>=0.22
# onnxruntime==1.30.0 already pinned in environment.yml (parity gate).
